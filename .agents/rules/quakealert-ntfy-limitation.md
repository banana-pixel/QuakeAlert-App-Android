---
trigger: always_on
---

This application is a fork of the open-source project ntfy (package: io.heckel.ntfy).
STRICT RULE: You are STRICTLY FORBIDDEN from touching, analyzing, or suggesting refactors for any code that belongs to the original ntfy project (including NotificationDao, the ntfy Service engine, and its default database schema).
You MUST ONLY focus on my custom code located in the id.my.bananapixel.quakealert package, specifically within the /api, /db (Quake/Chat only), /domain, /ui, and /util directories.